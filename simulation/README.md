# Simulation

```bash
npm install
```

Ensure server is running and information is correct in `config.js` file

```bash
npm run signup
```

After bots register as users. Create a room and paste your room code in `config.js` file

```bash
npm run sim
```
